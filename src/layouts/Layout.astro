---
import Navbar from "../components/Navbar.astro";
import NavMobile from "../components/NavMobile.astro";
import { ViewTransitions } from "astro:transitions";
import { fade } from "astro:transitions";

interface Props {
  title: string;
}

const { title } = Astro.props;

---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Bienvenidos a nuestra tienda de moda y estilo. Encontrarás una amplia selección de productos de alta calidad, desde bolsos elegantes y calzado cómodo hasta cosméticos de tendencia y ropa para todas las ocasiones. Además, no te pierdas nuestras misceláneas y accesorios únicos que añadirán un toque especial a tu estilo. ¡Explora y descubre lo que te hace sentir increíble! 🛍️✨" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/images/icon.png" />
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
	<ViewTransitions/>
  </head>
  <body transition:animate={fade({ duration: '0.4s' })}>
  <NavMobile/>
    <slot />
  </body>
  
</html>
<style is:global>
  html {
    font-family:
      Arial,
      Helvetica,
      sans-serif system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
    background-color: rgb(0,0 ,0);

  }
   *{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
   }

   

  body::-webkit-scrollbar{
    width: 0;
  }
  
  body::-webkit-scrollbar-thumb{
    background-color: rgb(2, 0, 13);
    border-radius: 10px;
  }
  
  body::-webkit-scrollbar-thumb:hover{
    background-color: rgb(2, 0, 13);
  }
  
</style>
